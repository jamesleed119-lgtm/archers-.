# Ragdoll Archers embed

This workspace contains a static HTML version of the provided embedded game page. Open the site locally with a web server.

## Run locally

```bash
cd /workspaces/archers-.
python3 -m http.server 8000
```

Then open http://localhost:8000

The page loads the Unity build from the CDN plus the ad banner logic from the supplied HTML.
